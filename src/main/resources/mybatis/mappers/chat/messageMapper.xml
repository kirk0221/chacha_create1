<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chacha.create.common.mapper.chat.MessageMapper">

	<resultMap id="messageResultMap" type="messageDTO">
		<id property="chatroomId" column="chatroom_id" />
		<result property="storeId" column="store_id" />
		<result property="memberId" column="member_id" />
		<result property="chattingText" column="chatting_text" />
	</resultMap>

	<select id="selectByChatroomId" resultMap="messageResultMap"
		parameterType="messageDTO">
		SELECT room.chatroom_id, room.member_id, room.store_id, chat.chatting_text FROM chatroom room LEFT JOIN chatting chat ON(room.chatroom_id = chat.chatroom_id) WHERE room.chatroom_id = #{chatroomId}
	</select>

	<select id="selectByMemberId"
		resultMap="messageResultMap" parameterType="int">
		SELECT room.chatroom_id, room.member_id, room.store_id, chat.chatting_text FROM chatroom room LEFT JOIN chatting chat ON(room.chatroom_id = chat.chatroom_id)WHERE room.member_id = #{memberId}
	</select>

	<select id="selectForMemberWithStoreAllMessage"
		resultMap="messageResultMap" parameterType="messageDTO">
		SELECT room.chatroom_id, room.member_id, room.store_id, chat.chatting_text FROM chatroom room LEFT JOIN chatting chat ON(room.chatroom_id = chat.chatroom_id)WHERE room.member_id = #{memberId} and room.store_id = #{storeId}
	</select>

	<select id="selectForMemberWithStoreURlAllMessage"
		resultMap="messageResultMap" parameterType="messageDTO">
		SELECT room.chatroom_id, room.member_id, room.store_id, chat.chatting_text FROM chatroom room LEFT JOIN chatting chat ON(room.chatroom_id = chat.chatroom_id)WHERE room.member_id = #{memberId} and room.store_id = (select store_id from store where store_url = #{storeUrl})
	</select>
	
	<select id="selectForGetChatRoomIdByStoreURl"
		resultMap="messageResultMap" parameterType="messageDTO">
		SELECT room.chatroom_id, room.member_id, room.store_id, chat.chatting_text FROM chatroom room WHERE room.member_id = #{memberId} and room.store_id = (select store_id from store where store_url = #{storeUrl})
	</select>

	<insert id="insertChatroom" parameterType="messageDTO">
		INSERT INTO chatroom (chatroom_id, store_id, member_id)
		VALUES (seq_chatroom_id.NEXTVAL, #{storeId}, #{memberId})
	</insert>

	<insert id="insertChatting" parameterType="messageDTO">
		INSERT INTO chatting (chatting_id, chatroom_id, chatting_text,
		chatting_date)
		VALUES (seq_chatroom_id.NEXTVAL, #{chatroomId}, #{chattingText}, sysdate)
	</insert>
	
</mapper>
